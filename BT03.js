let width = prompt("Nhập độ rộng của hình chữ nhật:");
let height = prompt("Nhập chiều cao của hình chữ nhật:");

let area = width * height;

document.write(`Diện tích của hình chữ nhật là: ${area}`);